# Report PDF — Fix Checklist

This file lists fixes for the **Word / PDF report** (`AI_Intrusion_Detection_System.pdf` / `ide_repoert2.pdf`) that **are not** automatically applied by editing `docs/PROJECT_REPORT.md`. Most of these are in the front matter or are PDF-formatting artefacts that the Word source needs to be patched for.

The Markdown source (`docs/PROJECT_REPORT.md`) has been updated separately with every substantive content fix (new CICIDS results, FPR honesty, threat model, adversarial section, baseline comparison, etc.). If the report's body was generated from the Markdown source, regenerate the PDF and the body fixes will land automatically. If the body was hand-typed in Word, you'll need to copy the new prose across.

---

## 1. Front matter — must-fix

### 1.1 Supervisor's name (spelling inconsistent)
- **Title page:** "Abhisek Roy"
- **Certificate:** "Abhisekh Roy"
- **Acknowledgement:** "Abhisekh Roy"

Pick one and use it consistently across all three pages. (Confirm spelling with your supervisor; "Abhisek" is the more common transliteration.)

### 1.2 Submission date (three different dates in front matter)
- **Title page:** "March, 2026"
- **Certificate:** "Date: 13-03-2026"
- **Acknowledgement:** "Date: 12/05/26" (May 12, 2026)

Pick the actual submission date and use it in all three places.

### 1.3 Acknowledgement — rewrite the first paragraph

Current text (broken grammar, wrong word choice):
> "The analysis of the project work wishes to express our gratitude to Abhisekh Roy for allowing the degree attitude and providing effective guidance in development of this project work. His conscription of the topic and all the helpful hints, he provided, contributed greatly to successful development of this work, without being pedagogic and overbearing influence."

Issues: "wishes" with plural subject "we"; "conscription" is the wrong word (means forced military draft); "without being pedagogic and overbearing influence" is grammatically broken.

Suggested replacement:
> "We wish to express our sincere gratitude to Abhisek Roy for his patient guidance throughout the development of this project. His suggestion of the topic and the steady stream of helpful hints he provided shaped this work without ever feeling pedagogic or overbearing."

---

## 2. Contents page

### 2.1 Page numbers in the TOC are off by one
- TOC says: Conclusion p22, References p23, Appendix A p24, Appendix B p25
- Actual: those sections start at p23, p24, p25, p26

If the TOC is auto-generated by Word, right-click → "Update field". If hand-typed, edit each line.

---

## 3. Section numbering bugs (Word auto-numbering reset issues)

### 3.1 Literature Survey sub-sections
Currently numbered **1.1, 1.2, 1.3** — but this is Section 2. Renumber to:
- 2.1 NSL-KDD Dataset
- 2.2 CICIDS-2017 Dataset
- 2.3 Related Algorithms

### 3.2 System Architecture sub-sections
Currently numbered **1.4, 1.5, 1.6, 1.7** — but this is Section 3. Renumber to:
- 3.1 High-Level Layout
- 3.2 Technology Stack
- 3.3 Design Rationale
- 3.4 Real-time Delivery

Word fix: select the heading style and reset numbering, or convert to manual numbering.

---

## 4. Body content — most-impact fixes

These items are **already fixed in `docs/PROJECT_REPORT.md`**. Replicate them in the Word source if the report wasn't generated from the Markdown.

### 4.1 System Architecture prose (pages 9–10) contradicts implementation
Current Word text says:
- "The backend is implemented in **Python** … packet capture libraries such as **libpcap or scapy**"
- "The data layer uses **PostgreSQL** to store network logs"

Both contradict the abstract, Technology Stack table, and the rest of the report. The system is pure-TypeScript + SQLite. The Word source still has leftover boilerplate from the original proposal — delete and replace with the corrected prose from `docs/PROJECT_REPORT.md` §3.

### 4.2 Section 7 (Results) needs a CICIDS subsection
The Word document's Section 7 only contains NSL-KDD results. Insert a new subsection **7.5 CICIDS-2017 Results** with the full table from `docs/PROJECT_REPORT.md` §9.5. Drop-in copy ready in the Markdown source.

### 4.3 FPR honesty paragraph
Add the new observation #3 from `docs/PROJECT_REPORT.md` §9.3 (the explicit FPR trade-off paragraph). One paragraph; takes the 18.41 % FPR claim from a celebration to an honest cost statement.

### 4.4 LSTM section honest reframe
Replace the existing soft hedge ("ships as a comparator") with the honest negative-result framing in `docs/PROJECT_REPORT.md` §4.5.

### 4.5 Active Learning — scope statement
Add the "Scope and current evaluation status" paragraph from §5. It honestly states that the AL loop is infrastructure pending empirical evaluation. Without this paragraph an examiner can reasonably ask "where are the AL results?"

### 4.6 New §11.6 Threat Model, §11.7 Adversarial, §11.8 TS-only limitations
All new in the Markdown source. Copy-paste-ready. ~1.5 pages of prose total.

### 4.7 Baseline comparison table
New §9.8 in the Markdown source — compares this work's 92.57 % F1 against published NSL-KDD baselines (Tavallaee 2009, Yin 2017 RNN-IDS, Vinayakumar 2019 deep CNN). Drops in cleanly.

### 4.8 Heartbleed taxonomy footnote
Both §2.2 and §7.3 now footnote that Heartbleed is technically a memory-disclosure vulnerability that the CIC release files under DoS for capture-day reasons.

### 4.9 CICIDS feature count
Current Word text claims 78 CICFlowMeter features uniformly. The actually-used Kaggle preprocessed mirror has 52 (with 26 redundant columns dropped). §2.2 and §9.5 in the new Markdown both footnote this honestly.

---

## 5. PDF formatting / layout fixes

### 5.1 ASCII architecture diagrams (pages 10, 18)
The box-drawn ASCII art doesn't render properly when Word exports to PDF — table cells split across lines. Replace with **proper figures** (PNG export from draw.io / Excalidraw / Mermaid). Source for a Mermaid version:

```mermaid
flowchart TB
  subgraph Browser["Browser — Next.js 16 (port 3000)"]
    UI["Dashboard · Detections · ML Models · Auto-Response · Training · Datasets · Alerts · AI Assistant"]
  end
  subgraph API["Next.js API Route Handlers (TypeScript, server-only)"]
    Routes["/api/{detect, attack, seed, detections, stats, blocked-ips, rlhf, training, metrics, alerts, auto-response, analyze, lstm, events}"]
  end
  subgraph Service["Service Layer (in-memory singletons)"]
    Detect[detection — ensemble.predict()]
    Auto[auto-response]
    Persist[persist → Prisma → SQLite]
    RLHF[rlhf — feedback → reweight ensemble]
    Train[auto-train — verified samples → retrain trigger]
  end
  subgraph ML["ML Layer (pure TypeScript, no Python, no GPU)"]
    IF["IsolationForest (30%)"]
    AE["Autoencoder (25%)"]
    RF["RandomForest (25%)"]
    XGB["XGBoost (20%)"]
    Ensemble["→ Ensemble"]
    LSTM["LSTM (separate /api/lstm)"]
    IF --> Ensemble
    AE --> Ensemble
    RF --> Ensemble
    XGB --> Ensemble
  end
  Browser -->|fetch · EventSource · Recharts| API
  API --> Service
  Service --> ML
```

Paste into the [Mermaid Live Editor](https://mermaid.live/) → export PNG → drop into Word.

### 5.2 Code snippets mangled mid-page (pages 16, 17, 23)
- Page 16: TypeScript switch block has no section header above it
- Page 17: `BlockedIP` token rendered into the middle of a sentence
- Page 23: `chrome.notifications` / `chrome.alarms` floating outside paragraph flow

These are likely Word's inline-code styling interacting with line wrap. Either:
- Use a monospace block with explicit padding (Insert → Text Box → Courier New), or
- Convert each problematic code snippet to a screenshot

### 5.3 No dashboard screenshots in Section 8
Two pages describe the dashboard tabs but show zero screenshots. The dashboard is the most demonstrable artefact in the project. **Mandatory:** add at least one full-page screenshot of the Dashboard tab, plus a screenshot of the Detections feed showing Confirm/Dismiss buttons.

Quick capture: `npm run dev`, browse to `http://localhost:3000`, hit `POST /api/seed` to populate demo data, then screenshot each tab.

### 5.4 No ML report figures
Standard practice for an empirical ML report — add at least:
- **Confusion matrix** as a heatmap (not just the 2×2 table)
- **ROC curve** for the ensemble + each individual model
- **F1 vs threshold sweep** (justifies the 0.35 choice from §9.6)
- **Per-attack-family recall bar chart** (visualises §9.5 numbers)

Quick way to generate: dump the test-set scores from `train-nslkdd.ts` to a CSV, plot in Python (matplotlib) or even Google Sheets.

---

## 6. Quick-win priority order

If time is short, do these six items first (highest impact per minute):

1. **Fix front matter** — supervisor name, dates, acknowledgement (15 min)
2. **Renumber sections 2 and 3** — Word auto-numbering reset (5 min)
3. **Insert the CICIDS results subsection** — copy from §9.5 in Markdown (10 min)
4. **Rewrite System Architecture prose** — copy from §3 in Markdown (10 min)
5. **Add the FPR honesty paragraph** — from §9.3 observation 3 (5 min)
6. **Add at least one dashboard screenshot** to Section 8 (15 min)

Total: ~1 hour. Everything else can follow.

---

## 7. Things deliberately NOT changed

- Headline numbers (90.99 % accuracy / 92.57 % F1 / 97.99 % recall) — unchanged from the source measurements.
- Ensemble weights (30/25/25/20) — unchanged from the implementation.
- Section count (12 sections in the Markdown; 11 in the Word doc because Discussion is folded into Conclusion). If you want the Word doc to match the Markdown's structure, add a new Section 11 "Discussion and Limitations" before Conclusion and move the threat-model / adversarial / TS-only content into it.

---

*Generated 2026-05-11 alongside the `docs/PROJECT_REPORT.md` content overhaul.*
