# Copy to .env. SQLite needs no setup beyond the file path.
# Path is relative to the project root (where you run npm/npx commands).
DATABASE_URL="file:./prisma/dev.db"

# Optional. Leave blank to use the offline AI stub.
GEMINI_API_KEY=""
